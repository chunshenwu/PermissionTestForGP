apply plugin: 'com.android.application'

android {
    signingConfigs {
        permissionTest {
            keyAlias 'permissionTest'
            keyPassword '123456'
            storeFile file('/Users/jason_wu/gp/permissionTest.keystore')
            storePassword '123456'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "24.0.0"
    defaultConfig {
        applicationId 'com.jason_wu.permissiontestforgp'
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 4
        versionName "4.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.permissionTest
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.4.0'
}
